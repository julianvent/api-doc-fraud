"""Liveness Detection (Face Anti-Spoofing) module.

WIP — NOT yet wired to the controller, router, or policy layer. Do not
import this package from outside `service/liveness/` until the lab/
subfolder is removed and integration is done deliberately.
"""
